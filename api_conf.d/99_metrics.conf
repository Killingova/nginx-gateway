# =========================================================
# 99_metrics.conf (server-level snippet)
# Rolle:
# - Public Listener soll KEINE Metrics ausliefern
# - Isolierte Metrics liegen auf separatem Server (conf.d/99_metrics.server.conf)
# =========================================================

location = /metrics {
  return 404;
}
